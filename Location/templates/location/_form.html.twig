<center>
    <div class="container">
        <div class="row g-5">
            <div class="container-xxl py-6">
{{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-outline-primary border-2">{{ button_label|default('Save') }}</button>
{{ form_end(form) }}
            </div>
        </div>
    </div>
</center>

